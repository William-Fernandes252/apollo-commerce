import { Typography } from '@mui/material';

export default function IndexPage() {
  return (
    <div>
      <Typography variant="body2" component={'p'}>
        Welcome to Apollo Commerce.
      </Typography>
    </div>
  );
}
